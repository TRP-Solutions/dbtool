{
	"name": "Example batch config",
	"host": "localhost",
	"user": "username",
	"password": "XXX",
	"foreign_permissions": "allow", // default is "deny"
	"batch": [
		{
			"database": "foo",
			"files": ["path/to/schema/file.sql"],
			"variables": {"VAR": "value"}
		},
		{
			"database": "bar",
			"files": ["path/to/schema/file.sql"],
			"variables": {"VAR": "value"}
		}
	]
	
}
